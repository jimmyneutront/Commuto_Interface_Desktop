package com.commuto.interfacedesktop.ui.swap

import androidx.compose.runtime.Composable

/**
 * Indicates the current [Composable] that should be focused in [SwapsComposable], appearing on the trailing side of the
 * [SwapsListComposable].
 */
enum class FocusedSwapComposable {
    SwapComposable,
}